# Copy to .env.local and fill in. NEVER commit .env.local — this repo is public.
# Rotate any previously exposed keys before use.
SERPER_API_KEY=
GROQ_API_KEY=
# Optional fallbacks (provider waterfall tries them in order):
GEMINI_API_KEY=
DEEPSEEK_API_KEY=
