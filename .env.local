# Copy to .env.local and fill in. NEVER commit .env.local — this repo is public.
SERPER_API_KEY=
GROQ_API_KEY=
# Optional waterfall fallbacks (tried in order: Groq → Gemini → OpenRouter → DeepSeek):
GEMINI_API_KEY=
OPENROUTER_API_KEY=
DEEPSEEK_API_KEY=
